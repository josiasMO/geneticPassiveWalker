from genetic import Individual, Environment
from simulation import simulation
from walker import walker
from math import *
from simulation import walker


s = simulation()
s.interactive()
#robot = walker(s.space, self._invy(event.pos), 80, 60, 10, pi/16, 0, 0, 0)
#s.step(0.02)
#robot = walker(s.space, (300,200), 80, 60, 10, pi/16, 0, 0, 0)
